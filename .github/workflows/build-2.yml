name: 正式版 Mesa (aarch64, Auto Update + Detailed Log, Stable Release)

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - 'refs/tags/**'
  schedule:
    # 每天北京时间早上 6 点（UTC+8 → UTC-8 = 22 点）
    - cron: "0 22 * * *"

permissions:
  contents: write
  actions: write

jobs:
  check-updates:
    runs-on: ubuntu-24.04-arm
    outputs:
      new_commit: ${{ steps.check.outputs.new_commit }}
      latest_tag: ${{ steps.check.outputs.latest_tag }}
    steps:
      - name: 检查 Mesa 最新正式稳定版
        id: check
        run: |
          echo "🔍 检查 Mesa 最新稳定版 Tag..."
          # 获取所有稳定版标签（格式：数字.数字.数字）
          LATEST_TAG=$(git ls-remote --tags https://gitlab.freedesktop.org/mesa/mesa.git | \
                       grep -E 'refs/tags/[0-9]+\.[0-9]+\.[0-9]+$' | \
                       awk -F'refs/tags/' '{print $2}' | \
                       sort -V | tail -n1)
          
          if [ -z "$LATEST_TAG" ]; then
            echo "❌ 无法获取最新稳定版标签"
            exit 1
          fi
          
          echo "最新稳定版 Tag: $LATEST_TAG"

          STATE_FILE=".mesa_latest_tag"
          if [ -f "$STATE_FILE" ]; then
            LAST_TAG=$(cat $STATE_FILE)
          else
            LAST_TAG=""
          fi
          echo "上次构建 Tag: $LAST_TAG"

          if [ "$LATEST_TAG" != "$LAST_TAG" ] || [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "✅ 检测到新版本或手动触发，准备构建"
            echo "new_commit=true" >> $GITHUB_OUTPUT
            echo "latest_tag=$LATEST_TAG" >> $GITHUB_OUTPUT
          else
            echo "❎ 没有新版本，跳过构建"
            echo "new_commit=false" >> $GITHUB_OUTPUT
          fi

  build:
    needs: check-updates
    if: ${{ needs.check-updates.outputs.new_commit == 'true' || github.event_name == 'workflow_dispatch' || github.event_name == 'push' }}
    runs-on: ubuntu-24.04-arm

    steps:
      - name: 检出仓库（包含状态文件）
        uses: actions/checkout@v4

      - name: 获取 Mesa 版本信息
        id: version
        run: |
          LATEST_TAG="${{ needs.check-updates.outputs.latest_tag }}"
          if [ -z "$LATEST_TAG" ] || [ "$LATEST_TAG" == "null" ]; then
            # 如果从 check-updates 获取失败，重新获取
            LATEST_TAG=$(git ls-remote --tags https://gitlab.freedesktop.org/mesa/mesa.git | \
                         grep -E 'refs/tags/[0-9]+\.[0-9]+\.[0-9]+$' | \
                         awk -F'refs/tags/' '{print $2}' | \
                         sort -V | tail -n1)
          fi
          
          if [ -z "$LATEST_TAG" ]; then
            echo "❌ 无法确定 Mesa 版本"
            exit 1
          fi
          
          echo "🎯 使用 Mesa 版本: $LATEST_TAG"
          echo "latest_tag=$LATEST_TAG" >> $GITHUB_OUTPUT
          echo "VERSION=$LATEST_TAG" >> $GITHUB_ENV

      - name: 安装依赖
        run: |
          sudo sed -i "s/^Types: deb$/Types: deb deb-src/" /etc/apt/sources.list.d/ubuntu.sources
          sudo apt update
          sudo apt install -y build-essential git cmake bison flex pkg-config \
              glslang-tools libxrandr-dev libxxf86vm-dev libxcb-shm0-dev libxcb-glx0-dev \
              libxext-dev libx11-dev libx11-xcb-dev libxcb1-dev libxcb-dri3-dev libxcb-present-dev \
              libxcb-randr0-dev libxcb-sync-dev libxcb-xfixes0-dev libxshmfence-dev libdrm-dev \
              libgbm-dev libegl1-mesa-dev libexpat1-dev libwayland-dev python3-pip
          pip3 install --user meson ninja mako
          echo "✅ 依赖安装完成"

      - name: 克隆 Mesa 最新稳定版
        run: |
          echo "📥 克隆 Mesa 仓库..."
          LATEST_TAG="${{ env.VERSION }}"
          echo "正在克隆标签: $LATEST_TAG"
          
          # 先克隆仓库，然后检出特定标签
          git clone --depth=1 https://gitlab.freedesktop.org/mesa/mesa.git mesa-source
          cd mesa-source
          
          # 检出特定标签
          if git checkout $LATEST_TAG; then
            echo "✅ 成功检出标签: $LATEST_TAG"
          else
            echo "❌ 无法检出标签 $LATEST_TAG，尝试获取完整历史"
            cd ..
            rm -rf mesa-source
            git clone https://gitlab.freedesktop.org/mesa/mesa.git mesa-source
            cd mesa-source
            git checkout $LATEST_TAG
          fi
          
          echo "🔧 构建版本信息:"
          git log -1 --oneline
          GIT_HASH=$(git rev-parse --short HEAD)
          echo "GIT_HASH=${GIT_HASH}" >> $GITHUB_ENV

      - name: 构建 Mesa
        run: |
          cd mesa-source
          echo "🏗️ 开始构建 Mesa..."
          
          # 创建必要的目录
          sudo mkdir -p /data/data/com.termux/files/usr/glibc
          sudo chmod 777 -R /data

          # 创建构建目录
          mkdir -p builddir
          
          # 配置构建选项
          meson setup builddir \
            --libdir=lib \
            -Dprefix=/data/data/com.termux/files/usr/glibc \
            -Dbuildtype=release \
            -Doptimization=3 \
            -Db_lto=true \
            -Dplatforms=x11 \
            -Degl-native-platform=x11 \
            -Dglx=dri \
            -Dglx-direct=true \
            -Dopengl=true \
            -Dgles1=enabled \
            -Dgles2=enabled \
            -Dglvnd=disabled \
            -Degl=enabled \
            -Dllvm=disabled \
            -Dshared-llvm=disabled \
            -Dshader-cache=enabled \
            -Dxlib-lease=enabled \
            -Dvulkan-beta=true \
            -Dgallium-drivers=zink,freedreno \
            -Dvulkan-drivers=freedreno \
            -Dfreedreno-kmds=msm,kgsl \
            -Dvideo-codecs=all \
            -Dgbm=enabled \
            -Dtools=drm-shim,freedreno

          # 构建
          echo "🔨 编译中..."
          ninja -C builddir -j$(nproc)
          
          # 安装到临时目录
          echo "📦 安装中..."
          DESTDIR=$(pwd)/install ninja -C builddir install
          
          echo "✅ Mesa 构建完成"

      - name: 打包构建产物
        run: |
          cd mesa-source
          TAR_NAME="termux-glibc-mesa-${{ env.VERSION }}-aarch64.tar.gz"
          
          # 打包所有安装的文件
          mkdir -p package
          cp -r install/data/data/com.termux/files/usr/glibc/* package/ || true
          
          # 特别包含 vulkan 驱动
          if [ -f builddir/src/freedreno/vulkan/libvulkan_freedreno.so ]; then
            mkdir -p package/lib
            cp builddir/src/freedreno/vulkan/libvulkan_freedreno.so package/lib/
          fi
          
          tar -czvf "../${TAR_NAME}" -C package .
          echo "TAR_NAME=${TAR_NAME}" >> $GITHUB_ENV
          echo "📦 打包完成: ${TAR_NAME}"

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: termux-glibc-mesa-${{ env.VERSION }}-aarch64
          path: ${{ env.TAR_NAME }}
          retention-days: 30

      - name: 发布到 GitHub Release
        if: ${{ github.event_name != 'schedule' }} # 定时任务不自动发布
        uses: softprops/action-gh-release@v1
        with:
          tag_name: mesa-${{ env.VERSION }}
          name: Mesa ${{ env.VERSION }} (aarch64, Termux glibc)
          body: |
            🔧 **Mesa 自动构建信息**
            
            - 🧱 版本: **${{ env.VERSION }}**
            - 💡 提交: `${{ env.GIT_HASH }}`
            - 📦 文件: `${{ env.TAR_NAME }}`
            - 🏗️ 架构: aarch64
            - 📋 平台: Termux glibc
            
            ## 驱动支持
            - ✅ Gallium: zink, freedreno
            - ✅ Vulkan: freedreno
            - ✅ OpenGL/OpenGL ES
          files: ${{ env.TAR_NAME }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 保存最新稳定版 Tag
        if: ${{ env.VERSION != '' && github.event_name != 'workflow_dispatch' }}
        run: |
          echo "${{ env.VERSION }}" > .mesa_latest_tag
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .mesa_latest_tag
          if ! git diff --cached --quiet; then
            git commit -m "Update Mesa latest stable tag to ${{ env.VERSION }}"
            git push
            echo "✅ 已保存最新版本标签"
          else
            echo "ℹ️ 无更改需要保存"
          fi
